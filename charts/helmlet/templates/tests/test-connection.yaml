apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmlet.fullname" . }}-test-connection"
  labels:
    {{- include "helmlet.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmlet.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
